/* implicit include of geometry_types.h */

__kernel void main(__global const struct snde_meshedpart *meshedparts,
		   __global const snde_coord *vertices,
		   __global const snde_index *vertexidx)
{
  
}

